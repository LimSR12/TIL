---
title: "스프링 부트 3 백엔드 개발자되기 - 01장"
summary: "자바 백엔드 개발자가 알아두면 좋은 지식"
status: publish
tag: [Spring Boot]
category: Backend
---

## 클라이언트란

## 서버란

## RDB

관계형 데이터베이스 (Relational DataBase)

## SQL

Structured Query Language

데이터 검색을 위한 언어

ANSI 표준 SQL이 있고 RDB 별로 조금씩 다른 문법도 있다

## NoSQL

RDB는 데이터 **저장, 조회, 수정, 삭제가 용이**하지만 **성능을 올리는게 쉽지 않다.**
데이터베이스의 성능을 높이려면 머신의 성능을 좋게하는 `Scale Up`, 머신을 여러대로 분리하는 `Scale Out` 이라는 기법이 있다.

`Scale Up` 은 장비를 업그레이드하면 쉽지만, `Scale Out` 은 데이터베이스 분산이 필요하다.
그리고 **이때 트랜잭션을 사용하면 성능이 떨어지게 되고**, 이러한 문제점을 해결하기 위해 **NoSQL** 이 등장했다.

## IP, Port

## 라이브러리

애플리케이션 개발에 필요한 기능인 클래스, 함수 등을 모아놓은 코드의 모음

## 프레임워크

소프트웨어 개발을 수월하게 하기 위한 소프트웨어 개발 환경.

일하기 위한(work) 틀(frame)을 제공하는 것!

> 프레임워크는 애플리케이션을 개발할 때 전체적인 구조를 잡기 위해 사용하는 것이고, 라이브러리는 개발을 하는 과정에서 필요한 기능을 구현하기 위해 사용한다.

## 자바 어노테이션

자바로 작성한 코드에 추가하는 표식
`@` 을 사용해 나타낸다.

다양한 목적으로 사용하지만, 보통 메타데이터로 사용하는 경우가 가장 많다.
